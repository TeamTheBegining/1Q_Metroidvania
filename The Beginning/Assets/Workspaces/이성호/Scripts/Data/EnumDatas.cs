using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// 범용 Enum 타입정의

public enum PoolType
{
    EnemyMelee = 0,
    HitFx,
    PoolTypeCount
}

public enum BGMType
{
    Menu = 0,
    Battle,
    BGMTypeCount
}

public enum SFXType
{ 
    Hit = 0,
    Attack,
    Jump,
    Step,
    SoundTypeCount
}